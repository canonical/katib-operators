type: charm
bases:
  - build-on:
    - name: "ubuntu"
      channel: "20.04"
    run-on:
    - name: "ubuntu"
      channel: "20.04"
parts:
  charm:
    # Pydantic build dependencies s
    build-packages: [cargo, rustc, pkg-config, libffi-dev, libssl-dev]
